# Скопируйте в .env и заполните реальными значениями:
#   cp .env.example .env
#
# Примечание: в репозитории также есть `env.example` для обратной совместимости.

# ===========================================================================
# OpenSearch
# ===========================================================================
OPENSEARCH_HOST=localhost
OPENSEARCH_PORT=9200
OPENSEARCH_USER=
OPENSEARCH_PASSWORD=
OPENSEARCH_USE_SSL=false
OPENSEARCH_INDEX=makar_cloud_semantic

# ===========================================================================
# Cloud.ru Foundation Models (OpenAI-совместимое API)
# ===========================================================================
# Укажите один из ключей (предпочтительно CLOUDRU_API_KEY; поддерживается и API_KEY)
CLOUDRU_API_KEY=
# API_KEY=

CLOUDRU_BASE_URL=https://foundation-models.api.cloud.ru/v1
CLOUDRU_EMBEDDING_MODEL=BAAI/bge-m3
CLOUDRU_CHAT_MODEL=zai-org/GLM-4.6
CLOUDRU_TEMPERATURE=0.5

# ВАЖНО: размерность должна соответствовать выбранной embedding-модели.
# Для BAAI/bge-m3 обычно 1024.
EMBEDDING_DIM=1024
